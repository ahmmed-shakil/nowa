import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const vendorCategorySlice = createSlice({
  name: "vendorCategory",
  initialState,
  reducers: {},
});

export const {} = vendorCategorySlice.actions;
export default vendorCategorySlice.reducer;
